import Vue from 'vue'
import Component from 'vue-class-component'

@Component({})
export default class HelloWorld extends Vue {
  private msg: string = "Hello World!!";
  private name: string = "yochidrop";
}
